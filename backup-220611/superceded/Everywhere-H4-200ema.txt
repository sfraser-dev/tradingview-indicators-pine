//@version=3
// adapted MTF EMA indicator
// H4-ema200 on all charts

study("H4 EMA 200 everywhere" , overlay=true)

mtf = input(title="MTF", type=resolution, defval="240")
ema_length = input(200, title = "EMA Period")

ema = security(tickerid, mtf, ema(close, ema_length))

p = plot(ema, title="", color=blue , linewidth=2, offset=0, transp=0)